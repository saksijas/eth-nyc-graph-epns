{
  "name": "lens-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ saksijas/subfluid",
    "create-local": "graph create --node http://localhost:8020/ saksijas/subfluid",
    "remove-local": "graph remove --node http://localhost:8020/ saksijas/subfluid",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 saksijas/subfluid"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0",
    "@protofire/subgraph-toolkit": "^0.1.2"
  }
}
